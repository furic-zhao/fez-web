<div class="fluid-container">
    <nav class="navbar navbar-default">
        <div class="navbar-header">
            <!-- Mobile Toggle Menu Button -->
            <a href="#" class="js-fez-nav-toggle fez-nav-toggle" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
              <i></i>
            </a>
            <a class="navbar-brand" href="/">
                <svg class="icon" aria-hidden="true">
                    <use xlink:href="#fez-fez-logo"></use>
                </svg>
            </a>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">
                {{#each this}}
                <li>
                    <a href="{{href}}" data-nav-section="{{section}}" class="{{class}}" target="{{target}}">
                        <span><i class="{{icon}}"></i> {{name}}</span>
                    </a>
                </li>
                {{/each}}

{{!--                 <li class="call-to-action">
                    <a class="sign-up" href="#">
                        <span>注册</span>
                    </a>
                </li>
                <li class="call-to-action">
                    <a class="log-in" href="#">
                        <span>登录</span>
                    </a>
                </li> --}}
            </ul>
        </div>
    </nav>
</div>
